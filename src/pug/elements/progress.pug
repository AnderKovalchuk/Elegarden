mixin progress(start, end, isLight= '')
    if isLight
        .progress.progress--light
            p.progress__num= start
            .progress__bar
            p.progress__num= end
    else
        .progress
            p.progress__num= start
            .progress__bar
            p.progress__num= end

